<Window x:Class="MiP.Ruler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ruler="clr-namespace:MiP.Ruler"
        mc:Ignorable="d"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        SnapsToDevicePixels="True"
        MouseLeftButtonDown="MainWindow_OnMouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseMove="Window_MouseMove"
        SizeChanged="Window_SizeChanged"
        ResizeMode="NoResize"
        Title="MainWindow"
        Width="525" Height="50" MinWidth="100" MinHeight="75"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        KeyDown="MainWindow_OnKeyDown"
        >

    <Grid Opacity="0.9">
        <Canvas Background="White" />
        <DockPanel LastChildFill="False">
            <ruler:TopRuler DockPanel.Dock="Top" Height="30" Background="White" />
            <ruler:BottomRuler DockPanel.Dock="Bottom" Height="15" Background="White" />
        </DockPanel>
        <ruler:BlackBorder Background="Transparent" />
        <ruler:RedLine Background="Transparent" />
    </Grid>
</Window>